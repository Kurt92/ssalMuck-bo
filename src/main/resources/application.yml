server:
  port : 8099

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: Wjdals12#
    driver-class-name: com.mysql.cj.jdbc.Driver
  #swagger setting
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true


logging:
  level:
    root: info
    org.hibernate.SQL: debug
    org.hibernate.type: trace

